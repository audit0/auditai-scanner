# Shared eval assets

- The two-tenant seed used by the sandbox lives in `packages/sandbox/scripts/seed-two-tenants.mjs` (mounted into the app container).
- `packages/sandbox/scripts/run-tests.sh` starts the app and runs a fixture's `security-test/` inside the container.
