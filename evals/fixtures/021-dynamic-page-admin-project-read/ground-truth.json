{
  "id": "021-dynamic-page-admin-project-read",
  "vulnerabilityExists": true,
  "class": ["BOLA", "IDOR", "multi-tenancy", "service-role-bypass"],
  "cwe": ["CWE-639", "CWE-284"],
  "severity": "critical",
  "stack": ["nextjs-app-router", "supabase", "supabase-ssr", "typescript"],
  "vulnerablePath": [
    "PAGE /projects/[id] (app/projects/[id]/page.tsx)",
    "createServerSupabaseClient().auth.getUser() authenticates the caller",
    "params.id (user-controlled)",
    "createAdminClient() (lib/supabase/admin.ts) bypasses RLS",
    "admin.from('projects').select('*').eq('id', id).single()"
  ],
  "expectedSecureBehavior": "A user from tenant A visiting /projects/<tenant B project id> gets a not-found page, never tenant B's project data.",
  "acceptableFixes": [
    "Render with the cookie client (createServerSupabaseClient) so the RLS policy scopes the row",
    "Keep the admin client but add an explicit tenant/ownership check before rendering"
  ],
  "unacceptableFixes": [
    "Weakening or dropping the RLS policy on projects",
    "Hiding the link to other tenants' projects in the UI while the page still renders them if visited directly"
  ]
}
