{
  "ruleId": "supabase.service-role-query-without-authentication",
  "title": "Unauthenticated service-role access to \"jobs\" in POST /api/cron/purge",
  "severity": "critical",
  "minConfidence": 0.8,
  "entrypoints": ["POST /api/cron/purge"],
  "sources": [],
  "sinks": ["supabase.delete:public.jobs"],
  "mustMentionFiles": ["vulnerable/app/api/cron/purge/route.ts"],
  "mustNotFlag": ["secure/app/api/cron/purge/route.ts"],
  "verification": {
    "strategy": "identity-matrix",
    "expectedBefore": "HTTP 200 anonymously, expired rows of every tenant deleted",
    "expectedAfter": "HTTP 401 anonymously, rows intact; HTTP 200 with the secret"
  }
}
