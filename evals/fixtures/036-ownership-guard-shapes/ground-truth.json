{
  "id": "036-ownership-guard-shapes",
  "vulnerabilityExists": true,
  "class": ["BOLA", "IDOR", "multi-tenancy", "service-role-bypass"],
  "cwe": ["CWE-639", "CWE-284"],
  "severity": "critical",
  "stack": ["nextjs-app-router", "supabase", "supabase-ssr", "typescript"],
  "vulnerablePath": [
    "GET /api/projects/[id]/tasks: tasks.select().eq('project_id', id) with no read of the parent project",
    "PATCH /api/documents/[id]: the document is read by id, only its existence is checked, then updated",
    "GET /api/reports/[id]?all=1: the owner filter is skipped whenever the caller sends ?all=1",
    "createAdminClient() bypasses RLS in all three"
  ],
  "expectedSecureBehavior": "Bob gets 404 on Alice's project tasks, Alice's document rename and Alice's report; Alice's own requests return 200 and her rename persists.",
  "acceptableFixes": [
    "Read the parent project with an owner filter and stop with 404 before reading its tasks",
    "Compare the document's owner_id with the caller in code and stop with 404 before the update",
    "Add the owner filter unconditionally, or under a condition the caller cannot steer (app_metadata role)"
  ],
  "unacceptableFixes": [
    "A parent read that ignores an empty result",
    "An owner comparison that logs instead of returning",
    "A condition read from the request (query string, body, header) deciding whether to scope"
  ]
}
