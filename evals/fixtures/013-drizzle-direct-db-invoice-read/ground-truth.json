{
  "id": "013-drizzle-direct-db-invoice-read",
  "vulnerabilityExists": true,
  "class": [
    "BOLA",
    "IDOR",
    "multi-tenancy",
    "direct-db-bypasses-rls"
  ],
  "cwe": [
    "CWE-639",
    "CWE-284"
  ],
  "severity": "critical",
  "expectedSecureBehavior": "A user from tenant A requesting an invoice that belongs to tenant B receives 403 or 404 and no invoice data.",
  "unacceptableFixes": [
    "Relying on Supabase RLS while still querying through the direct connection",
    "Filtering in JavaScript after reading the row"
  ],
  "stack": [
    "nextjs-app-router",
    "drizzle",
    "postgres",
    "supabase-auth",
    "typescript"
  ],
  "vulnerablePath": [
    "GET /api/invoices/[id] (app/api/invoices/[id]/route.ts)",
    "params.id (user-controlled)",
    "db (lib/db.ts) direct Postgres connection, RLS does not apply",
    "db.select().from(invoices).where(eq(invoices.id, id))"
  ],
  "acceptableFixes": [
    "Scope the query by the caller's tenant id taken from their profile",
    "Run the query under the authenticated role with request.jwt.claims set so RLS applies"
  ]
}
