{
  "id": "031-guard-read-then-service-role-delete",
  "vulnerabilityExists": true,
  "class": ["BOLA", "IDOR", "multi-tenancy", "service-role-bypass"],
  "cwe": ["CWE-639", "CWE-284"],
  "severity": "critical",
  "stack": ["nextjs-app-router", "supabase", "supabase-ssr", "typescript"],
  "vulnerablePath": [
    "DELETE /api/flows/[id]",
    "cookie client auth.getUser() proves who is calling, not what they own",
    "createAdminClient() bypasses RLS",
    "flows.delete().eq('id', id) with no tenant scope"
  ],
  "expectedSecureBehavior": "Bob deleting Alice's flow gets 404 and the row stays; Alice deleting her own flow gets 200.",
  "acceptableFixes": [
    "Read the row through the caller's RLS-scoped client first and return 404 when it is missing (guard read), then delete",
    "Scope the delete itself by the caller's tenant_id",
    "Delete through the user-scoped client so RLS enforces ownership"
  ],
  "unacceptableFixes": [
    "A guard read through the service-role client",
    "A guard read whose empty result is ignored"
  ]
}
