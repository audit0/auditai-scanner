{
  "id": "020-formdata-server-action-delete",
  "vulnerabilityExists": true,
  "class": ["BOLA", "IDOR", "multi-tenancy", "service-role-bypass"],
  "cwe": ["CWE-639", "CWE-284"],
  "severity": "critical",
  "stack": ["nextjs-app-router", "supabase", "supabase-ssr", "typescript", "server-actions"],
  "vulnerablePath": [
    "server action deleteDocument(formData), bound to <form action={deleteDocument}>",
    "formData.get('id') assigned to a local const id (user-controlled)",
    "createServiceRoleClient() bypasses RLS",
    "documents.delete().eq('id', id)"
  ],
  "expectedSecureBehavior": "Bob invoking deleteDocument with Alice's document id leaves the row in place; anonymous callers are rejected.",
  "acceptableFixes": [
    "Authenticate inside the action and scope the delete to the caller's tenant",
    "Use a user-scoped client so RLS enforces ownership"
  ],
  "unacceptableFixes": ["Hiding the button in the UI"],
  "note": "Detected since 12 September 2026 (receiver taint: formData.get() on a tainted FormData). Promoted from evals/pending; see README.md."
}
