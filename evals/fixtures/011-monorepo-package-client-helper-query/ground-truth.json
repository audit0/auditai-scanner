{
  "id": "011-monorepo-package-client-helper-query",
  "vulnerabilityExists": true,
  "class": [
    "BOLA",
    "IDOR",
    "multi-tenancy",
    "service-role-bypass"
  ],
  "cwe": [
    "CWE-639",
    "CWE-284"
  ],
  "severity": "critical",
  "stack": [
    "nextjs-app-router",
    "supabase",
    "typescript",
    "monorepo"
  ],
  "vulnerablePath": [
    "GET /api/invoices/[id] (apps/web/app/api/invoices/[id]/route.ts)",
    "params.id (user-controlled)",
    "getSupabaseServerAdminClient() (packages/supabase/src/clients/server-admin-client.ts) bypasses RLS",
    "loadInvoice(client, id) (packages/invoices/src/server.ts)",
    "client.from('invoices').select('*').eq('id', invoiceId)"
  ],
  "expectedSecureBehavior": "A user from tenant A requesting an invoice that belongs to tenant B receives 403 or 404 and no invoice data.",
  "acceptableFixes": [
    "Pass the caller's user-scoped client into the helper so RLS applies, and scope by tenant_id",
    "Keep the admin client but add an explicit tenant check before returning data"
  ],
  "unacceptableFixes": [
    "Weakening or dropping the RLS policy",
    "Filtering in JavaScript after reading the row"
  ]
}
